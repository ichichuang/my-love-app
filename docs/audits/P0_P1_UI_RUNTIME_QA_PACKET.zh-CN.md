# P0/P1 UI 修复手工 QA 包

## 1. 范围确认

- 生成日期：2026-06-26
- 类型：手工 QA 执行表；未代替微信开发者工具或真机验证结果。
- 当前 P0/P1 修复表面：小宠物导航、创建页离开/保存/图片/日期流程、详情日期展示、任务日期清空、日期工具与日期字段组件。
- 创建本文档前 `git status --short`：
  - `M src/components/AppPetNavigator.vue`
  - `M src/pages.json`
  - `M src/pages/create/create.vue`
  - `M src/pages/detail/detail.vue`
  - `M src/pages/task-edit/task-edit.vue`
  - `M src/utils/date.ts`
  - `?? src/components/AppDateField.vue`
  - `?? src/static/pet/`
- 结论：除本文档外，已检查到的代码/资源改动均落在当前 P0/P1 UI 修复集；未发现不相关源码文件混入。

## 2. 执行前置

- 微信开发者工具导入 `dist/build/mp-weixin` 或通过项目脚本生成的 mp-weixin 产物。
- 至少一台真机完成预览：iOS 或 HarmonyOS 可作为普通真机，Android 真机必须覆盖系统返回。
- CloudBase 环境可用，能创建、读取、删除 `love_entries` 数据，并能检查 `love-entries/main/` 私有文件。
- QA 执行时不要改源码；如需验证小宠物图片失败兜底，只能在一次性构建产物或 DevTools 资源拦截中模拟，完成后通过重新构建恢复产物。

## 3. 手工 QA 矩阵

结果栏只填 `PASS`、`FAIL` 或 `NA`。证据栏填写截图、录屏、CloudBase 控制台记录、DevTools console 摘要或备注。

| ID | 场景 | 步骤 | 精确期望结果 | DevTools 结果 | 真机结果 | 证据 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| QA-01 | 首页宠物图片渲染 | 打开首页，等待小宠物出现。 | 显示 `pet-idle.png`，无破图图标、无空白圆、无布局跳动；点击区域仍在页面右下浮层范围内。 |  |  |  |  |
| QA-02 | 宠物 fallback badge | 在 DevTools 资源拦截或一次性构建产物中让 `/static/pet/pet-idle.png` 加载失败后打开首页。 | 小宠物位置显示圆形 fallback badge，中心为“珊”；不显示破图图标；菜单、拖拽、导航仍可用。 |  |  |  |  |
| QA-03 | 宠物拖拽、吸附、菜单导航 | 首页拖动宠物到屏幕左右/上下边缘，松手；点击打开菜单，进入写回忆、小歌单、小约定、设置任一路由。 | 拖动过程跟手；松手后吸附到安全边界，不遮挡顶部胶囊和底部操作；菜单展开不溢出；点击菜单进入正确页面；返回后宠物状态可继续交互。 |  |  |  |  |
| QA-04 | 创建页脏离开保护 | 进入创建页，输入标题或正文，点击自定义返回或“先不写了”。 | 出现 Wot MessageBox，标题为“这张纸条还没收好”；点“继续写”留在创建页且内容保留；点“不保存离开”离开页面。 |  |  |  |  |
| QA-05 | 创建页保存中导航锁 | 创建页填写必填项并上传图片，点击保存后立即点返回或“先不写了”。 | 保存中不离开页面；出现“正在轻轻收好，先等它一下下”Toast；保存请求不重复提交；保存完成后按正常成功路由跳转。 |  |  |  |  |
| QA-06 | 图片上传后放弃清理 | 创建页上传 1 张以上图片，不保存，触发离开确认并确认放弃；随后检查 CloudBase 存储。 | 页面离开成功；未创建新回忆；已上传但未保存的文件不会长期残留在 `love-entries/main/`，若清理异步执行，必须有重试/清理证据且用户不看到技术错误。 |  |  |  |  |
| QA-07 | 带图片成功保存 | 创建页填写标题、正文、日期，上传 1 至 3 张图片后保存。 | 保存按钮进入加载态；保存成功后回到首页或目标页；新回忆出现且图片可显示/预览；CloudBase 文档包含 `coupleId: "main"`，图片 fileID 为私有文件。 |  |  |  |  |
| QA-08 | 日期选择器打开/确认/取消 | 创建页、详情编辑页、任务编辑页分别点击日期字段；选择日期后确认；再次打开后取消。 | 日期弹层可打开；确认按钮文案“就这天”，取消按钮文案“再想想”；确认后字段显示中文日期；取消后保留原值不变；无英文文案。 |  |  |  |  |
| QA-09 | 任务日期清空按钮 | 任务编辑页选择日期后，点击日期字段右侧清空按钮。 | 日期值被清空；字段回到“可以先空着”；保存任务时允许空日期；清空按钮不误触打开日期弹层。 |  |  |  |  |
| QA-10 | 详情页中文日期显示 | 打开已有回忆详情，记录日期为 `2026-06-26`。 | 查看态日期显示为 `2026年6月26日`；非法或缺失旧值不崩溃，必要时保留原始值；编辑保存后仍显示中文日期。 |  |  |  |  |
| QA-11 | 深色模式 | 在设置切换深色模式，回到首页、创建页、详情页、任务编辑页。 | 背景、纸条、输入框、宠物、日期字段、Toast、MessageBox 对比度正常；无黑字压深色背景、无白块突兀、无图标丢失。 |  |  |  |  |
| QA-12 | 大字号 | 在设置切换最大字号，覆盖首页、宠物菜单、创建页、详情页、任务编辑页。 | 文案不互相覆盖；按钮文字不截断；日期字段和任务清空按钮仍可读可点；宠物菜单不超出屏幕。 |  |  |  |  |
| QA-13 | 紧凑密度 | 在设置切换紧凑密度，覆盖首页、创建页、详情页、任务编辑页。 | 间距变紧但不拥挤；触控目标仍可点击；日期字段、保存按钮、返回按钮不重叠。 |  |  |  |  |
| QA-14 | 窄屏 | DevTools 选择 320 至 360 宽度机型，真机使用窄屏或分屏；覆盖首页、创建页、任务编辑页。 | 宠物不挡主要 CTA；菜单可完整显示；表单单列降级稳定；日期字段文本省略合理；没有横向滚动或 WXSS 布局错位。 |  |  |  |  |
| QA-15 | 键盘打开/关闭 | 创建页和详情编辑页聚焦标题、正文、心情字段，打开并收起键盘。 | 页面滚动/避让稳定；当前输入不被键盘盖住；保存、返回、日期字段位置不跳乱；收起后布局恢复。 |  |  |  |  |
| QA-16 | Toast 生命周期 | 触发保存成功、保存中返回、表单校验失败、删除成功。 | Toast 使用 Wot 样式；显示在目标页或当前页的稳定宿主中；不因路由跳转被截断；不会残留遮挡下一次操作。 |  |  |  |  |
| QA-17 | Wot MessageBox 生命周期 | 触发创建页脏离开、详情/任务删除或离开确认；分别点取消和确认。 | MessageBox 只出现一次；取消后关闭并保留页面状态；确认后执行对应离开/删除；路由变化后无半透明遮罩残留。 |  |  |  |  |
| QA-18 | 自定义导航返回 | 在创建页输入内容后点击顶部自定义返回；详情编辑、任务编辑也覆盖对应返回入口。 | 有未保存内容时先弹确认；取消留在原页；确认才离开；无未保存内容时直接返回。 |  |  |  |  |
| QA-19 | Android 系统返回 | Android 真机进入创建页并输入内容，按系统返回键；保存中再次按系统返回。 | 有未保存内容时系统返回被拦截并弹同一确认；保存中系统返回被锁定并提示等待；确认放弃后才离开。 | NA |  |  | Android 真机必测。 |
| QA-20 | 保存/删除后的路由反馈 | 分别执行创建保存、详情保存、详情删除、任务保存/删除；观察返回目标页。 | 目标页显示对应成功 Toast；列表/首页数据同步刷新；Toast 不在源页面销毁时消失；删除后不能再从列表进入已删除记录。 |  |  |  |  |

## 4. 最终判定

- 最终结论：`未执行`
- 可选值：`PASS_RUNTIME_QA`、`PASS_WITH_MINOR_POLISH`、`FAIL_NEEDS_FIX`
- 判定人：
- 判定时间：
- 阻塞项：
- 可接受的小问题：

判定规则：

- `PASS_RUNTIME_QA`：QA-01 至 QA-20 在必测平台全部 PASS，且无 P0/P1 运行时问题。
- `PASS_WITH_MINOR_POLISH`：核心流程 PASS，仅存在不阻断发布的 P2/P3 视觉或文案小问题，并已记录证据。
- `FAIL_NEEDS_FIX`：任一 P0/P1 场景 FAIL，包括保存/删除丢数据、图片清理失败且不可恢复、返回保护失效、Toast/MessageBox 阻断操作、真机布局严重重叠。
