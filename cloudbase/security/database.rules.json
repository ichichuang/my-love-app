{
  "read": "(auth.openid == 'OWNER_OPENID' || auth.openid == 'PARTNER_OPENID')",
  "create": "(auth.openid == 'OWNER_OPENID' || auth.openid == 'PARTNER_OPENID') && doc.coupleId == 'main'",
  "update": "(auth.openid == 'OWNER_OPENID' || auth.openid == 'PARTNER_OPENID')",
  "delete": "(auth.openid == 'OWNER_OPENID' || auth.openid == 'PARTNER_OPENID')"
}
