{
  "read": "(auth.openid == 'OWNER_OPENID' || auth.openid == 'PARTNER_OPENID') && /^love-entries\\/main\\//.test(resource.path)",
  "write": "(auth.openid == 'OWNER_OPENID' || auth.openid == 'PARTNER_OPENID') && /^love-entries\\/main\\//.test(resource.path)"
}
